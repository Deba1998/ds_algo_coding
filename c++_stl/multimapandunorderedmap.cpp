//multimap
//everything is same as map only it can store multiple keys in sorted order
//mp[key] cannot be used here

//unorderedmap
//not sorted and unique elements
//all operations are same and max operations take O(1) time in worst case O(n) as compared to normal map's  O(logn)